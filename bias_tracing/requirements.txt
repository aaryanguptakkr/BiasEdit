# Pinned stack of the conda env `bias_trace_olmo` (Python 3.10.19, CUDA 12.1) that produced
# ALL bias_tracing results (OLMo-2, Pythia, Qwen2.5, Llama-3.2, Gemma-3 runs, 2026-07/08).
# Gemma 3 needs transformers >= 4.50; the repo-root requirements.txt describes the editing
# pipeline (main.py / hydra) and was never used for bias_tracing.
torch==2.5.1
transformers==5.3.0
tokenizers==0.22.2
safetensors==0.7.0
huggingface-hub==1.6.0
datasets==4.6.1
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.8
tqdm==4.67.3
PyYAML==6.0.3
