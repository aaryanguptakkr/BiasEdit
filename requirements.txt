# Editing pipeline (main.py / hydra) dependencies, inherited from upstream zjunlp/BiasEdit.
# bias_tracing/ (causal tracing) uses its OWN pinned stack: see bias_tracing/requirements.txt
# (transformers 5.3.0 / torch 2.5.1 — the versions that produced all tracing results).
datasets==2.17.1
hydra-core==1.3.2
matplotlib==3.8.3
numpy==1.26.4
omegaconf==2.3.0
protobuf==4.25.3
sentencepiece==0.1.99
scikit-learn==1.4.2
scipy==1.12.0
torch==2.0.1
transformers==4.45.1
wandb==0.16.3
